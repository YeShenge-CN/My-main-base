# ===========================================================================
# DeepSeek 知识问答小助手 —— 依赖清单
#
# 安装：
#     pip install -r requirements.txt
#
# 说明：
#     - PyQt5       : 桌面图形界面
#     - openai      : 调用 DeepSeek 的 OpenAI 兼容接口
#     - python-dotenv: 从 .env 读取 API Key
#
# PyInstaller 仅打包时需要，不属于运行时依赖，因此单独列出。
# ===========================================================================

PyQt5>=5.15.9
openai>=1.30.0
python-dotenv>=1.0.0

# ---- 打包用（可选）----
# pyinstaller>=6.0.0
